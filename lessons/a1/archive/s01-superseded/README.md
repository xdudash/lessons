# Superseded S01 lessons

These 30 lesson files (`L11`–`L40`) were generated under the previous fixed-slot/quota model.

They are preserved as historical work so that no source material is lost, but they are **not accepted as production-final content** under Course Architecture v2.

The current production S01 scope is the rebuilt `L01`–`L10` in the parent directory. Any future S01 continuation must be explicitly rebuilt/reviewed under Architecture v2 rather than copied from this archive.
